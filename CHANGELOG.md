# 更新日志

## 2026-07-26 · 本地图片上传 + AI 联网采集改写 + 排版优化

本次更新在保留原有架构（Next.js App Router + JSON 数据层 + Markdown 文章）与全部基础功能的前提下，完成三项升级。

### 📝 新增：Markdown 编辑器插入本地图片

- 三种插入方式，均自动上传并在光标处插入 `![说明](/uploads/xxx.png)`：
  - 工具栏「🖼 上传图片」按钮选择本地文件（可多选）；
  - 把图片**拖拽**进正文输入框；
  - 直接**粘贴截图**。
- 「图库」面板：查看全部已上传图片，点击即插入复用，悬浮可删除。
- 实时 Markdown **预览**，与前台文章页同一套排版样式。
- 快捷排版工具栏：小标题 / 加粗 / 引用 / 列表 / 代码块 / 链接 / 分隔线。
- 存储与访问：图片保存在 `data/uploads/`（随 Docker 卷 `haizhuai-data` 持久化），
  由新路由 `/uploads/[...path]` 对外提供访问，带路径穿越防护与长缓存；
  上传接口仅管理员可用，限 8MB，允许 PNG / JPG / GIF / WebP / AVIF。

相关文件：`app/api/admin/upload/route.ts`、`app/uploads/[...path]/route.ts`、`app/admin/page.tsx`

### 🔎 新增：AI 联网采集一键改写（后台新标签页）

面向管理员的资料收集工作流，三步生成文章：

1. **联网搜索**：关键词检索网页 / 论坛 / 新闻（DuckDuckGo HTML 版为主、Bing 兜底，
   无需搜索 API 密钥），内置常用预设；也可直接粘贴文章 / 帖子网址。
2. **抓取正文**：勾选结果（单次最多 6 篇）一键抓取，自动剥离脚本、导航、页脚等
   噪音，提取纯文本（单篇截断 8000 字）。
3. **AI 改写**：复用站内 `/api/chat` —— 即前台 AI 对话配置的**同一套 API 渠道与
   模型映射**，无需额外密钥；流式生成含标题 / 摘要 / 分类的 Markdown 稿件
   （小标题分节、要点列表、引用块、表格、文末参考来源），
   支持附加写作要求，生成后「一键填入文章编辑器」直接进入发布流程。

相关文件：`app/api/admin/collect/route.ts`、`app/admin/page.tsx`（CollectPanel）

### 🎨 优化：UI 适配与排版

- 文章正文（`.prose-haizhu`）：
  - 图片 / 视频 / iframe 自适应宽度，圆角卡片式呈现，iframe 默认 16:9；
  - 表格斑马纹、表头底色，窄屏可横向滚动不撑破布局；
  - 长链接 / 长单词自动断行，移动端不再溢出；
  - 补全正文内 h1 / h4 / hr 样式。
- 管理后台：标签栏在移动端改为横向滑动；采集页与编辑器表单窄屏自动纵向堆叠。

相关文件：`app/globals.css`、`app/admin/page.tsx`

### 其他

- `README.md`：补充新功能说明与 `data/uploads/` 备份提示。
- `docker-compose.yml`：卷注释注明上传图片同样落在 `haizhuai-data` 卷中。

### 升级说明

- 无破坏性变更，无新增依赖，直接拉取代码重新构建即可。
- 使用「AI 采集改写」第 ③ 步前，需先在后台「🤖 AI 模型」页配置至少一个
  OpenAI 兼容 / Ollama 渠道（与前台对话共用）。
- 服务器需可访问外网（DuckDuckGo / Bing 及目标网页）才能使用联网搜索与抓取。
